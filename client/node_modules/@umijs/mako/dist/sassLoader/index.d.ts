import { type Options } from 'sass';
declare function sassLoader(
  fn: Function | null,
  opts: Options<'async'>,
): {
  render: (filePath: string) => Promise<any>;
  terminate: () => void;
};
export { sassLoader };
